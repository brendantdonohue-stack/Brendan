# Copy this file to config/config.yaml and fill in your own values,
# OR set the equivalent environment variables (which always take priority --
# see README, this is what the GitHub Actions workflow uses via repo secrets).
#
#   SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, ALERT_FROM, ALERT_TO

smtp_host: smtp.gmail.com
smtp_port: 465
smtp_user: your-gmail-address@gmail.com
# Use a Gmail "App Password", not your real password:
# https://myaccount.google.com/apppasswords
smtp_password: REPLACE_ME
alert_from: your-gmail-address@gmail.com
alert_to: brendandon@gmail.com
